# Project Overview

The Customer Health Projection Agent is an AI-assisted web application that will detect early customer escalation risk from configured project signals.

MVP implementation is limited to GitHub polling and Admin-entered deliverables. Scores must remain deterministic, explainable, and linked to source evidence.

Phase 00 only establishes the project foundation.
